# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Supabase CLI local cache (project link state, pooler URL, version pins)
supabase/.temp/

# Synthetic QA credentials (never real user data, never committed)
.qa.local.json

# Schema-only backups of the new Supabase project (Fase 5) -- local only,
# never committed even though they contain no secrets/data, to avoid
# accidental drift between what's backed up and what's in migrations.
.local-backups/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
